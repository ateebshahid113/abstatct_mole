<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_school_student_form" model="ir.ui.view">
        <field name="name">school.student.form</field>
        <field name="model">school.student</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="email"/>
                        <field name="phone"/>
                        <field name="age"/>
                        <field name="grade"/>
                        <field name="room_id"/>
                        <field name="active"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_school_student_list" model="ir.ui.view">
        <field name="name">school.student.list</field>
        <field name="model">school.student</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="email"/>
                <field name="phone"/>
                <field name="age"/>
                <field name="grade"/>
                <field name="room_id"/>
            </list>
        </field>
    </record>

    <record id="action_school_student" model="ir.actions.act_window">
        <field name="name">Students</field>
        <field name="res_model">school.student</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_active': 1}</field>
    </record>

    <record id="view_school_student_list_with_wizard" model="ir.ui.view">
        <field name="name">school.student.list.wizard</field>
        <field name="model">school.student</field>
        <field name="inherit_id" ref="view_school_student_list"/>
        <field name="arch" type="xml">
            <list position="inside">
                <header>
                    <button name="%(action_student_teacher_wizard)d" string="Assign Teacher" type="action"/>
                </header>
            </list>
        </field>
    </record>
</odoo>
